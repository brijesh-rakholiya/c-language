#include<stdio.h>
#include<conio.h>

main()
{
	int radius;
	float pi=3.14,area;
	clrscr();

	printf("Enter the radius of the circle : ");
	scanf("%d",&radius);
	area=pi*radius*radius;

	printf("The area of the circle is : %f",area);

	getch();
}